camoufox==0.4.11
requests==2.33.1
python-dotenv==1.2.2

# System dependencies (replit.nix):
# gtk3, glib, dbus-glib, alsa-lib, xorg.libXrender, xorg.libXt
# Required for camoufox headless Firefox to run without a display.
# Without gtk3: "libgtk-3.so.0: cannot open shared object file" error.
camoufox
python-dotenv
requests
